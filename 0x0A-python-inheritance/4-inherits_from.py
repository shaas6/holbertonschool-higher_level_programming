#!/usr/bin/python3
"""Checks the instance of a class"""


def inherits_from(obj, a_class):
    """Returns false if class and obj match"""
    if type(obj is a_class:
        return False
    return (issubclass(type(obj), a_class))
